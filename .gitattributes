# Patch context must preserve exact upstream bytes (Lua LF, Irrlicht mixed).
/tools/patches/**/*.patch -text
# Windows cmd.exe requires this launcher to retain CRLF after checkout.
/tools/Uninstall-UndoMod.cmd text eol=crlf
