--- a/include/IGUIEnvironment.h
+++ b/include/IGUIEnvironment.h
@@ -86,6 +86,10 @@
 	\return True on success, false on failure */
 	virtual bool setFocus(IGUIElement* element) = 0;
 
+	//! Transaction install only: caller pauses input and retains both widget trees.
+	//! No events or allocations; ordinary setFocus behavior is unchanged.
+	virtual void installPreparedFocus(IGUIElement* element) noexcept = 0;
+
 	//! Returns the element which holds the focus.
 	/** \return Pointer to the element with focus. */
 	virtual IGUIElement* getFocus() const = 0;
